grep Assignment Answers

1. grep \Z cities.txt

2. grep gan$ cities.txt

3. grep ^[ADE] cities.txt

4. grep '\<.ar' cities.txt

5. grep ^[^C] cities.txt

6. egrep -i '^[a-z\-]{14,}$' cities.txt

7. grep -c '^B' cities.txt

--this only returns 8, even if non-case sensitive, only returns 12.
8. egrep -c '(.*d){2}' cities.txt

9. egrep '(.)\1+$' cities.txt

10. egrep -i '^(.)(.).\2\1$' cities.txt